$(document).ready(function(){   

 var item = $('.item'),
    slideName = $('.slide_name'),
//     nameTrig = $('#name_trig').children().length;
   
     for (var i, i < slideName.length, i++){
    if (item.hasClass('active')){
       slideName[i].addClass('highlight');
    }};
    
    console.log(nameTrig);
}); 